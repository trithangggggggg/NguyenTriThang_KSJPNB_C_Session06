#include <stdio.h>

int main() {
    int number[5];
    int Tong = 0;

    printf("Nhap 5 so nguyen:\n");
        scanf("%d", &number[i]);
        if (numbers[i] % 2 != 0) {
            Tong += number[i];
        }
    }
    printf("Tong cac so le: %d\n", Tong);
    return 0;
}
